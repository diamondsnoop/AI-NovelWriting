# V0.4.0-0.6.0 API恢复联合验收文档

## 0. 目的

在 API 恢复后，基于**全新项目**完成 `V0.4.0 ~ V0.6.0` 的联合质量验收，确认：

- `V0.4.0` 的检索增强、伏笔审查接入、plan上层能力与结构化摘要字段可用；
- `V0.5.0` 的 `high_point / reader_pull / gatekeeper 分级` 行为符合冻结设计；
- `V0.6.0` 的 `decision / presentation / learn` 结构在真实链路中可用。

---

## 1. 验收环境与样本

- 执行日期（北京时间）：2026-03-12
- 模型配置：`openai / gpt-5.4 / chat_completions`
- 验收项目（全新）：`.tmp_acceptance_runs/acceptance_v04_v06`
- 章节样本：连续 `3` 章（每章执行 `plan + write`）
- 证据文件：`reports/开发基线文档/V0.4.0-0.6.0_API恢复联合验收结果.json`

说明：

- 本次未复用 `demo_real`、`acceptance_v03` 等历史项目，避免历史数据污染验收结论。

---

## 2. 执行步骤

1. API 调用连通性验证
- 最小 `LLMClient` 调用返回 `API_OK`。
- 实际 `plan` 命令可成功返回并落盘 outline/volume_plan/beats/timeline。

2. 全新项目联合跑批
- 新建 `acceptance_v04_v06`。
- 连续执行 3 轮 `plan + write`，采集 review/gatekeeper/decision/retrieval/summaries/foreshadowing 证据。

3. V0.6 特殊覆盖
- `presentation` 读前后文件哈希比对，验证只读边界。
- 执行 `learn` 并查询 `project_memory`。
- 使用结构化模拟输入覆盖 `decision.trigger_engine` 三类触发码。

---

## 3. 总结论

联合验收结果：`PASS`

- `V0.4.0`：通过
- `V0.5.0`：通过
- `V0.6.0`：通过

状态快照：

- 进度：`completed_chapters = 3`
- `tasks_count = 4`
- `decisions_count = 5`
- `entities_count = 20`
- `foreshadowing_count = 11`
- `summaries_count = 3`
- `summary_sync_issue_count = 0`
- `project_memory_count = 1`
- `status_report.risk_level = info`

---

## 4. 分版本结论

## 4.1 V0.4.0（长篇记忆增强）

验收项与结果：

- `plan` 输出非空：通过
- `retrieval_route` 在写作组包中存在：通过
- 结构化摘要中 `high_point_markers / reader_pull_marker` 字段存在且可解析：通过
- `foreshadowing_checker` 已接入审查：通过

证据要点：

- 3章均生成完整 `outline/compact/volume_plan/beats/timeline`；
- 检索召回片段数：第1章 `0`（首章无历史上下文，符合预期）、第2章 `2`、第3章 `4`；
- `foreshadowing_checker` 在第2/3章给出 warning（冲突风险提示），第1章 info。

## 4.2 V0.5.0（六维审查成型）

验收项与结果：

- `high_point` checker 接入：通过
- `reader_pull` checker 接入：通过
- `gatekeeper` 分级字段与理由字段存在：通过
- 非阻断型 checker 的 critical 不会直接强拦截：通过

证据要点：

- 3章 gate 结果均为 `pass + warning`；
- `high_point` 均给出 warning（密度/对齐问题）；
- `reader_pull` 均为 info（`reader_pull_present`）；
- 未出现“仅因 `high_point/reader_pull` 而 blocked”的违规行为。

## 4.3 V0.6.0（用户协作与展示）

验收项与结果：

- `presentation` 只读边界：通过（查询前后文件哈希一致）
- `learn` 写入与查询：通过（可写入并查询 `project_memory`）
- `decision` 三类触发覆盖：通过
- 语义决策记录写入：通过

证据要点：

- 实跑第3章已出现 `setting_conflict`、`route_branch`；
- 通过结构化模拟补齐 `review_severe_issue`，三类触发码完整覆盖：
  - `review_severe_issue`
  - `setting_conflict`
  - `route_branch`

---

## 5. 风险与后续动作

1. `foreshadowing_checker` 误报率仍需扩大样本评估
- 当前样本仅 3 章，能确认“接入有效”，但不足以给出稳定误报率结论。
- 建议在同一项目上追加到 8~10 章后统计 warning 的人工复核准确率。

2. `test_openai_integration` 用例被跳过
- 原因是该测试通过 `OPENAI_API_KEY` 环境变量装饰器判定，当前执行上下文未注入该变量。
- 本次已通过真实 `plan/write` 链路完成替代验证，不影响本次联合结论。

---

## 6. 最终结论

`V0.4.0 ~ V0.6.0` 在 API 恢复后的联合验收已通过，当前可以进入下一版本开发。

本轮结论属于“结构+真实链路已验证通过”；后续仅需按既定调整范围继续做质量标定与样本扩展。
